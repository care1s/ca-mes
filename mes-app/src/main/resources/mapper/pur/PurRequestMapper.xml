<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carels.mes.module.pur.mapper.PurRequestMapper">

    <resultMap type="com.carels.mes.module.pur.domain.PurRequest" id="PurRequestResult">
        <id property="requestId" column="request_id"/>
        <result property="requestCode" column="request_code"/>
        <result property="requestDate" column="request_date"/>
        <result property="requestType" column="request_type"/>
        <result property="applicantId" column="applicant_id"/>
        <result property="applicantName" column="applicant_name"/>
        <result property="deptId" column="dept_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="currency" column="currency"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectPurRequestVo">
        select request_id, request_code, request_date, request_type, applicant_id, applicant_name,
               dept_id, dept_name, total_amount, currency, status, remark, create_by, create_time,
               update_by, update_time
        from pur_request
    </sql>

    <select id="selectPurRequestList" parameterType="com.carels.mes.module.pur.domain.PurRequest" resultMap="PurRequestResult">
        <include refid="selectPurRequestVo"/>
        <where>
            <if test="requestCode != null and requestCode != ''">
                AND request_code like concat('%', #{requestCode}, '%')
            </if>
            <if test="requestType != null and requestType != ''">
                AND request_type = #{requestType}
            </if>
            <if test="applicantName != null and applicantName != ''">
                AND applicant_name like concat('%', #{applicantName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="requestDate != null">
                AND request_date = #{requestDate}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectPurRequestById" resultMap="PurRequestResult">
        <include refid="selectPurRequestVo"/>
        where request_id = #{requestId}
    </select>

    <select id="selectPurRequestByCode" resultMap="PurRequestResult">
        <include refid="selectPurRequestVo"/>
        where request_code = #{requestCode}
    </select>

    <select id="selectPendingRequests" resultMap="PurRequestResult">
        <include refid="selectPurRequestVo"/>
        where status = 'PENDING'
        order by request_date desc
    </select>

    <insert id="insertPurRequest" parameterType="com.carels.mes.module.pur.domain.PurRequest" useGeneratedKeys="true" keyProperty="requestId">
        insert into pur_request (
            request_code, request_date, request_type, applicant_id, applicant_name,
            dept_id, dept_name, total_amount, currency, status, remark, create_by, create_time
        ) values (
            #{requestCode}, #{requestDate}, #{requestType}, #{applicantId}, #{applicantName},
            #{deptId}, #{deptName}, #{totalAmount}, #{currency}, #{status}, #{remark}, #{createBy}, sysdate()
        )
    </insert>

    <update id="updatePurRequest" parameterType="com.carels.mes.module.pur.domain.PurRequest">
        update pur_request
        <set>
            <if test="requestCode != null">request_code = #{requestCode},</if>
            <if test="requestDate != null">request_date = #{requestDate},</if>
            <if test="requestType != null">request_type = #{requestType},</if>
            <if test="applicantId != null">applicant_id = #{applicantId},</if>
            <if test="applicantName != null">applicant_name = #{applicantName},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="deptName != null">dept_name = #{deptName},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="currency != null">currency = #{currency},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where request_id = #{requestId}
    </update>

    <delete id="deletePurRequestById">
        delete from pur_request where request_id = #{requestId}
    </delete>

    <delete id="deletePurRequestByIds">
        delete from pur_request where request_id in
        <foreach collection="array" item="requestId" open="(" separator="," close=")">
            #{requestId}
        </foreach>
    </delete>

    <update id="updateRequestStatus">
        update pur_request
        set status = #{status}, update_time = sysdate()
        where request_id = #{requestId}
    </update>

    <select id="selectMaxRequestCodeByDate" resultType="java.lang.String">
        SELECT MAX(request_code) FROM pur_request WHERE request_code LIKE CONCAT('PRQ', #{dateStr}, '%')
    </select>

</mapper>
