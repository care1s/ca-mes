<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carels.mes.module.pur.mapper.PurReturnMapper">

    <resultMap type="com.carels.mes.module.pur.domain.PurReturn" id="PurReturnResult">
        <id property="id" column="id"/>
        <result property="returnNo" column="return_no"/>
        <result property="receiptId" column="receipt_id"/>
        <result property="receiptNo" column="receipt_no"/>
        <result property="vendorId" column="vendor_id"/>
        <result property="vendorCode" column="vendor_code"/>
        <result property="vendorName" column="vendor_name"/>
        <result property="returnDate" column="return_date"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="totalQuantity" column="total_quantity"/>
        <result property="returnReason" column="return_reason"/>
        <result property="status" column="status"/>
        <result property="auditStatus" column="audit_status"/>
        <result property="stockOutStatus" column="stock_out_status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectPurReturnVo">
        select id, return_no, receipt_id, receipt_no, vendor_id, vendor_code, vendor_name,
               return_date, total_amount, total_quantity, return_reason, status, audit_status,
               stock_out_status, remark, create_by, create_time, update_by, update_time
        from pur_return
    </sql>

    <select id="selectPurReturnList" parameterType="com.carels.mes.module.pur.domain.PurReturn" resultMap="PurReturnResult">
        <include refid="selectPurReturnVo"/>
        <where>
            <if test="returnNo != null and returnNo != ''">
                AND return_no like concat('%', #{returnNo}, '%')
            </if>
            <if test="receiptNo != null and receiptNo != ''">
                AND receipt_no like concat('%', #{receiptNo}, '%')
            </if>
            <if test="vendorName != null and vendorName != ''">
                AND vendor_name like concat('%', #{vendorName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="auditStatus != null">
                AND audit_status = #{auditStatus}
            </if>
            <if test="stockOutStatus != null">
                AND stock_out_status = #{stockOutStatus}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectPurReturnById" resultMap="PurReturnResult">
        <include refid="selectPurReturnVo"/>
        where id = #{returnId}
    </select>

    <select id="selectPurReturnByCode" resultMap="PurReturnResult">
        <include refid="selectPurReturnVo"/>
        where return_no = #{returnCode}
    </select>

    <select id="selectReturnsByReceipt" resultMap="PurReturnResult">
        <include refid="selectPurReturnVo"/>
        where receipt_id = #{receiptId}
        order by return_date desc
    </select>

    <insert id="insertPurReturn" parameterType="com.carels.mes.module.pur.domain.PurReturn" useGeneratedKeys="true" keyProperty="id">
        insert into pur_return (
            return_no, receipt_id, receipt_no, vendor_id, vendor_code, vendor_name,
            return_date, total_amount, total_quantity, return_reason, status, audit_status,
            stock_out_status, remark, create_by, create_time
        ) values (
            #{returnNo}, #{receiptId}, #{receiptNo}, #{vendorId}, #{vendorCode}, #{vendorName},
            #{returnDate}, #{totalAmount}, #{totalQuantity}, #{returnReason}, #{status}, #{auditStatus},
            #{stockOutStatus}, #{remark}, #{createBy}, sysdate()
        )
    </insert>

    <update id="updatePurReturn" parameterType="com.carels.mes.module.pur.domain.PurReturn">
        update pur_return
        <set>
            <if test="returnNo != null">return_no = #{returnNo},</if>
            <if test="receiptId != null">receipt_id = #{receiptId},</if>
            <if test="receiptNo != null">receipt_no = #{receiptNo},</if>
            <if test="vendorId != null">vendor_id = #{vendorId},</if>
            <if test="vendorCode != null">vendor_code = #{vendorCode},</if>
            <if test="vendorName != null">vendor_name = #{vendorName},</if>
            <if test="returnDate != null">return_date = #{returnDate},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="totalQuantity != null">total_quantity = #{totalQuantity},</if>
            <if test="returnReason != null">return_reason = #{returnReason},</if>
            <if test="status != null">status = #{status},</if>
            <if test="auditStatus != null">audit_status = #{auditStatus},</if>
            <if test="stockOutStatus != null">stock_out_status = #{stockOutStatus},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deletePurReturnById">
        delete from pur_return where id = #{returnId}
    </delete>

    <delete id="deletePurReturnByIds">
        delete from pur_return where id in
        <foreach collection="array" item="returnId" open="(" separator="," close=")">
            #{returnId}
        </foreach>
    </delete>

    <update id="updateReturnStatus">
        update pur_return
        set status = #{status}, update_time = sysdate()
        where id = #{returnId}
    </update>

</mapper>
