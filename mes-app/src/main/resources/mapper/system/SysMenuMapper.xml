<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carels.mes.module.system.mapper.SysMenuMapper">

    <resultMap type="com.carels.mes.module.system.domain.SysMenu" id="SysMenuResult">
        <id property="menuId" column="menu_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectSysMenuVo">
        select menu_id, menu_name, parent_id, order_num, path, component, menu_type,
               visible, status, perms, icon, remark, create_by, create_time, update_by, update_time
        from sys_menu
    </sql>

    <select id="selectSysMenuList" parameterType="com.carels.mes.module.system.domain.SysMenu" resultMap="SysMenuResult">
        <include refid="selectSysMenuVo"/>
        <where>
            <if test="menuName != null and menuName != ''">
                AND menu_name like concat('%', #{menuName}, '%')
            </if>
            <if test="menuType != null and menuType != ''">
                AND menu_type = #{menuType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by order_num asc
    </select>

    <select id="selectSysMenuById" resultMap="SysMenuResult">
        <include refid="selectSysMenuVo"/>
        where menu_id = #{menuId}
    </select>

    <select id="selectAllMenus" resultMap="SysMenuResult">
        <include refid="selectSysMenuVo"/>
        where status = 0
        order by order_num asc
    </select>

    <select id="selectMenusByRoleId" resultMap="SysMenuResult">
        select m.* from sys_menu m
        inner join sys_role_menu rm on m.menu_id = rm.menu_id
        where rm.role_id = #{roleId} and m.status = 0
        order by m.order_num asc
    </select>

    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        select distinct m.* from sys_menu m
        inner join sys_role_menu rm on m.menu_id = rm.menu_id
        inner join sys_user_role ur on rm.role_id = ur.role_id
        where ur.user_id = #{userId} and m.status = 0
        order by m.order_num asc
    </select>

    <insert id="insertSysMenu" parameterType="com.carels.mes.module.system.domain.SysMenu" useGeneratedKeys="true" keyProperty="menuId">
        insert into sys_menu (
            menu_name, parent_id, order_num, path, component, menu_type,
            visible, status, perms, icon, remark, create_by, create_time
        ) values (
            #{menuName}, #{parentId}, #{orderNum}, #{path}, #{component}, #{menuType},
            #{visible}, #{status}, #{perms}, #{icon}, #{remark}, #{createBy}, sysdate()
        )
    </insert>

    <update id="updateSysMenu" parameterType="com.carels.mes.module.system.domain.SysMenu">
        update sys_menu
        <set>
            <if test="menuName != null">menu_name = #{menuName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="path != null">path = #{path},</if>
            <if test="component != null">component = #{component},</if>
            <if test="menuType != null">menu_type = #{menuType},</if>
            <if test="visible != null">visible = #{visible},</if>
            <if test="status != null">status = #{status},</if>
            <if test="perms != null">perms = #{perms},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where menu_id = #{menuId}
    </update>

    <delete id="deleteSysMenuById">
        delete from sys_menu where menu_id = #{menuId}
    </delete>

    <update id="updateStatus">
        update sys_menu
        set status = #{status}, update_time = sysdate()
        where menu_id = #{menuId}
    </update>

</mapper>
