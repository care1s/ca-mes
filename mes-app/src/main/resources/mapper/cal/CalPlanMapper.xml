<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carels.mes.module.cal.mapper.CalPlanMapper">

    <resultMap type="com.carels.mes.module.cal.domain.CalPlan" id="CalPlanResult">
        <id property="planId" column="plan_id"/>
        <result property="planCode" column="plan_code"/>
        <result property="planName" column="plan_name"/>
        <result property="planDate" column="plan_date"/>
        <result property="shiftId" column="shift_id"/>
        <result property="shiftCode" column="shift_code"/>
        <result property="shiftName" column="shift_name"/>
        <result property="teamId" column="team_id"/>
        <result property="teamCode" column="team_code"/>
        <result property="teamName" column="team_name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCalPlanVo">
        select p.plan_id, p.plan_code, p.plan_name, p.plan_date, p.shift_id, p.shift_code, p.shift_name,
               p.team_id, p.team_code, p.team_name, p.status, p.remark, p.create_by, p.create_time,
               p.update_by, p.update_time
        from cal_plan p
    </sql>

    <select id="selectCalPlanList" parameterType="com.carels.mes.module.cal.domain.CalPlan" resultMap="CalPlanResult">
        <include refid="selectCalPlanVo"/>
        <where>
            <if test="planCode != null and planCode != ''">
                AND p.plan_code like concat('%', #{planCode}, '%')
            </if>
            <if test="planName != null and planName != ''">
                AND p.plan_name like concat('%', #{planName}, '%')
            </if>
            <if test="shiftId != null">
                AND p.shift_id = #{shiftId}
            </if>
            <if test="teamId != null">
                AND p.team_id = #{teamId}
            </if>
            <if test="beginDate != null">
                AND p.plan_date &gt;= #{beginDate}
            </if>
            <if test="endDate != null">
                AND p.plan_date &lt;= #{endDate}
            </if>
            <if test="status != null and status != ''">
                AND p.status = #{status}
            </if>
        </where>
        order by p.plan_date, p.shift_id
    </select>

    <select id="selectCalPlanById" resultMap="CalPlanResult">
        <include refid="selectCalPlanVo"/>
        where p.plan_id = #{planId}
    </select>

    <insert id="insertCalPlan" parameterType="com.carels.mes.module.cal.domain.CalPlan" useGeneratedKeys="true" keyProperty="planId">
        insert into cal_plan (plan_code, plan_name, plan_date, shift_id, shift_code, shift_name,
                              team_id, team_code, team_name, status, remark, create_by, create_time, update_by, update_time)
        values (#{planCode}, #{planName}, #{planDate}, #{shiftId}, #{shiftCode}, #{shiftName},
                #{teamId}, #{teamCode}, #{teamName}, #{status}, #{remark}, #{createBy}, now(), #{updateBy}, now())
    </insert>

    <insert id="batchInsert">
        insert into cal_plan (plan_code, plan_name, plan_date, shift_id, shift_code, shift_name,
                              team_id, team_code, team_name, status, remark, create_by, create_time, update_by, update_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.planCode}, #{item.planName}, #{item.planDate}, #{item.shiftId}, #{item.shiftCode}, #{item.shiftName},
             #{item.teamId}, #{item.teamCode}, #{item.teamName}, #{item.status}, #{item.remark}, #{item.createBy}, now(), #{item.updateBy}, now())
        </foreach>
    </insert>

    <update id="updateCalPlan" parameterType="com.carels.mes.module.cal.domain.CalPlan">
        update cal_plan
        <set>
            <if test="planCode != null and planCode != ''">plan_code = #{planCode},</if>
            <if test="planName != null and planName != ''">plan_name = #{planName},</if>
            <if test="planDate != null">plan_date = #{planDate},</if>
            <if test="shiftId != null">shift_id = #{shiftId},</if>
            <if test="shiftCode != null">shift_code = #{shiftCode},</if>
            <if test="shiftName != null">shift_name = #{shiftName},</if>
            <if test="teamId != null">team_id = #{teamId},</if>
            <if test="teamCode != null">team_code = #{teamCode},</if>
            <if test="teamName != null">team_name = #{teamName},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_by = #{updateBy},
            update_time = now()
        </set>
        where plan_id = #{planId}
    </update>

    <delete id="deleteCalPlanById">
        delete from cal_plan where plan_id = #{planId}
    </delete>

    <delete id="deleteCalPlanByIds">
        delete from cal_plan where plan_id in
        <foreach collection="array" item="planId" open="(" separator="," close=")">
            #{planId}
        </foreach>
    </delete>

    <select id="checkPlanUnique" resultType="int">
        select count(*) from cal_plan where plan_date = #{planDate} and shift_id = #{shiftId}
    </select>

</mapper>
